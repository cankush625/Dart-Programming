// Dart requires the dart:core package. This package is imported in every program automatically by default
// import 'dart:core';

void main() { // The entry point of the dart code
  print("Hello World"); // Printing the string 

  // Defining variable using var keyword. Here the dart compliler automatically guess the type of the variable
  var firstName = "Ankush";
  // Providing the type of the compiler using the specific type like int, String, etc
  String lastName = "Chavan";

  // Concatinating the strings
  print(firstName + " " + lastName);
}